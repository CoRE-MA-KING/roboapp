[Unit]
Description=Roboapp LiDAR Sender {{LIDAR_NAME}}

[Service]
ExecStart={{APP_FOLDER}}/lidar_system/build/sender -n {{LIDAR_NAME}}
ExecStop=/bin/kill ${MAINPID}
Restart=always
RestartSec=3

[Install]
WantedBy=default.target